services:
  devcontainer:
    environment:
      # Tell sops where to find the secret key for age decryption.
      SOPS_AGE_KEY_FILE: /workspace/.sops.key
    image: git.zarantonello.dev/infra/ci:v2.3.80@sha256:0b210de67ce3b37089a3574fc05adc382ad993ec15edc4e9ac5a33b2be7982da
    volumes:
      - ..:/workspace:cached

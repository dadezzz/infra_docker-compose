services:
  devcontainer:
    environment:
      # Tell sops where to find the secret key for age decryption.
      SOPS_AGE_KEY_FILE: /workspace/.sops.key
    image: git.zarantonello.dev/infra/ci:v2.3.28@sha256:6719981841d32bf346067093f73257cc74fedecc7be7119882f3f3e7ebd86b90
    volumes:
      - ..:/workspace:cached

services:
  devcontainer:
    environment:
      # Tell sops where to find the secret key for age decryption.
      SOPS_AGE_KEY_FILE: /workspace/.sops.key
    image: git.zarantonello.dev/infra/ci:v2.3.78@sha256:b572aecaaeaa6c02dbbb6886fdd129bce8ec512235d3584705dfefede4ff9c09
    volumes:
      - ..:/workspace:cached

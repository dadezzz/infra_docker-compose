services:
  devcontainer:
    environment:
      # Tell sops where to find the secret key for age decryption.
      SOPS_AGE_KEY_FILE: /workspace/.sops.key
    image: git.zarantonello.dev/infra/ci:v2.3.36@sha256:a58db4bd43908e682c8396cffdaadc977aec92cd4d55a05c117a907615b74833
    volumes:
      - ..:/workspace:cached

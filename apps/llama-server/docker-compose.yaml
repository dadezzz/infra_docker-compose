networks:
  traefik_default:
    external: true

services:
  llama-server:
    command: |
      -fit off
      --no-mmap
      -b 1024 -ub 256
      --cache-ram 4096
      --models-max 1
      --models-preset /var/lib/llama-server/models.ini
    devices:
      - /dev/dri
    image: ghcr.io/ggml-org/llama.cpp:server-vulkan@sha256:db55ad27a96cbe5c025d1933626bdc1f006cb4ebe6a6c3fd16c8a179e3688890
    labels:
      traefik.enable: true
      traefik.http.routers.llama-server.entrypoints: http,https
      traefik.http.routers.llama-server.rule: Host(`llama.zarantonello.dev`)
      traefik.http.services.llama-server.loadbalancer.server.port: 8080
    networks:
      - traefik_default
    read_only: true
    restart: always
    user: 1000:1000
    volumes:
      - $HOME/volumes/llama-server/cache:/home/ubuntu/.cache:z,U
      - $HOME/volumes/llama-server/models:/var/lib/llama-server:z,ro

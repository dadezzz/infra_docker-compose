cache:
  dir: /data/cache

container:
  network: forgejo-runner_default
  # Mount pod's /etc/hosts to keep the git.zarantonello.dev address override.
  options: -v /etc/hosts:/etc/hosts
  privileged: true
  valid_volumes:
    - /etc/hosts
    - forgejo-runner-cache-*

host:
  workdir_parent: /data/job

log:
  job_level: info
  level: info

runner:
  capacity: 1
  fetch_interval: 30s

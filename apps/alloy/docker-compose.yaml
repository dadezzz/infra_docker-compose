configs:
  alloy:
    file: ./_configs/config.alloy

networks:
  alloy_default:
    name: alloy_default

services:
  alloy:
    cap_drop:
      - ALL
    command:
      - run
      - --server.http.listen-addr=[::]:12345
      - --storage.path=/var/tmp/alloy
      - --server.http.enable-pprof=false
      - /etc/alloy/config.alloy
    configs:
      - source: alloy
        target: /etc/alloy/config.alloy
    environment:
      NODE_HOSTNAME: ${HOSTNAME}
    extra_hosts:
      - hosts.docker.internal:host-gateway
    group_add:
      - 991
    image: docker.io/grafana/alloy:v1.16.3@sha256:84b76d56c5943025515438fd45cbb5995386a0260d072c9b671dfd7f539f9c01
    labels:
      monitoring.logs: true
      monitoring.metrics.port: 12345
    networks:
      - alloy_default
    read_only: true
    restart: always
    security_opt:
      - no-new-privileges:true
    user: 58210:58210
    volumes:
      - alloy-wal:/var/tmp/alloy
      - /var/run/docker.sock:/var/run/docker.sock

volumes:
  alloy-wal: {}
